import React from "react";
import { Link } from "react-router-dom";
import ImageSlider from "./ImageSlider";

export default function CardItems({ product }) {
  return <div>{/* <ImageSlider images={product.imag bves} /> */}</div>;
}
